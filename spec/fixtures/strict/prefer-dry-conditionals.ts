/* tslint:disable */
/* tslint:enable:prefer-dry-conditionals */
(() => {
  if (obj.name === "Stuff") {
    doStuff();
  } else if (obj.name === "Other") {
    doOtherStuff();
  } else {
    doFallback();
  }
})
